import type { NextPage } from "next";
import Layout from "../layouts/Layout";

const Home: NextPage = () => {
    return (
        <Layout>
            <div className=" p-4 lg:px-12">Home</div>
        </Layout>
    );
};

export default Home;
